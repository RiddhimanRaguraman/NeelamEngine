#include "pch.h"
#include "Transform.h"

Neelam::transform::transform_id Neelam::transform::create_transform(const init_info, GameEntity::entity_id entity_id)
{
    return transform_id();
}

void Neelam::transform::remove_transform(transform_id id)
{
}
