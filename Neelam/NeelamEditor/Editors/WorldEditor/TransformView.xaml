<UserControl x:Class="NeelamEditor.Editors.TransformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NeelamEditor.Editors"
             xmlns:vm="clr-namespace:NeelamEditor.Components"
             xmlns:utl="clr-namespace:NeelamEditor.Utilities.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MSTransform}"
             d:DesignHeight="200" d:DesignWidth="300">
    <local:ComponentView Header="Transform">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Position" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>
            <utl:VectorBox X="{Binding PosX}" Y="{Binding PosY}" Z="{Binding PosZ}"
                           Grid.Column="1" Grid.Row="0"
                           PreviewMouseLeftButtonDown="OnPosition_VectorBox_PreviewMouse_LBD"
                           PreviewMouseLeftButtonUp="OnPosition_VectorBox_PreviewMouse_LBU"
                           LostKeyboardFocus="OnPosition_VectorBox_LostKeyboardFocus"/>

            <TextBlock Text="Rotation" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center"/>
            <utl:VectorBox X="{Binding RotX}" Y="{Binding RotY}" Z="{Binding RotZ}"
                           Grid.Column="1" Grid.Row="1"
                           PreviewMouseLeftButtonDown="OnRotation_VectorBox_PreviewMouse_LBD"
                           PreviewMouseLeftButtonUp="OnRotation_VectorBox_PreviewMouse_LBU"
                           LostKeyboardFocus="OnRotation_VectorBox_LostKeyboardFocus"/>

            <TextBlock Text="Scale" Grid.Column="0" Grid.Row="2" VerticalAlignment="Center"/>
            <utl:VectorBox X="{Binding ScaleX}" Y="{Binding ScaleY}" Z="{Binding ScaleZ}"
                           Grid.Column="1" Grid.Row="2"
                           PreviewMouseLeftButtonDown="OnScale_VectorBox_PreviewMouse_LBD"
                           PreviewMouseLeftButtonUp="OnScale_VectorBox_PreviewMouse_LBU"
                           LostKeyboardFocus="OnScale_VectorBox_LostKeyboardFocus"/>
        </Grid>
    </local:ComponentView>
</UserControl>
