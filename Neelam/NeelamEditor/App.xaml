<Application x:Class="NeelamEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NeelamEditor"
             Startup="OnStartup">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Order matters. EditorColors holds the color/brush/font tokens
                     that everything else resolves against, so it merges FIRST.
                     DarkTheme (visual styles) references those tokens; then
                     ControlTemplates references TextBoxStyle from DarkTheme. -->
                <ResourceDictionary Source="Themes/EditorColors.xaml"/>
                <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
                <ResourceDictionary Source="Themes/ControlTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
